//
// Created by Bourne on 2017/3/13.
//

#include "book1_002.h"
#include <cctype>
using namespace std;

// 需要处理替换成 "%20" 之后长度变长的问题
void Solution::replaceSpace(char *str, int length)
{
    for(int i = 0; i < length; i++){
        if(isspace(str[i])){
            str[i] =
        }

    }
}
